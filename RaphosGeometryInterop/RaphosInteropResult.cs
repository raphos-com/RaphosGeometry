namespace Raphos.Geometry.Interop
{
    /// <summary>
    /// Mirrors the native RAPHOS_ERROR (0) / RAPHOS_SUCCESS (1) return codes.
    /// </summary>
    public enum RaphosInteropResult
    {
        Error,
        Success,
    }
}
